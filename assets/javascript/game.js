const authors = ["agatha", "stephen", "monteiro", "clarice", "lewis"];

//Choose randomly one author from the array
let author = authors[Math.floor(Math.random() * authors.length)];
console.log(author); 

//Capture input from user from text box
var userInput = document.getElementById('user_guess').value;
console.log(userInput);

//Iterate over the 
